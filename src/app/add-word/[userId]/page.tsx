
import React from 'react';

export default async function Page() {
    // const currentUser = await getUserById( await params[ 'userId' ] );

    // console.log( JSON.stringify( currentUser ) );
    // console.log( JSON.stringify( currentUser.topics ) );


    return (
        <>
            <h1>Add word page</h1>
            {/* {currentUser ? <AddWord */}
            {/* serializedUser={JSON.stringify( currentUser )} */}
            {/* serializedTopics={JSON.stringify( currentUser.topics )} */}
            {/* /> : <div>...</div>} */}
        </>
    )
}